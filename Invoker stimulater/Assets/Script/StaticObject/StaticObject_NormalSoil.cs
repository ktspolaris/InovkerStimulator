using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaticObject_NormalSoil : StaticObject
{
    // Start is called before the first frame update
    void Start()
    {
        dry = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
